name: Debug
on:
  status
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - id: debug
        uses: hmarr/debug-action@v2
      - run: echo "Success!"
        if: github.event.pull_request && github.event.state == 'success'
